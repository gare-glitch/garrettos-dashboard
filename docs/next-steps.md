# Next steps

- Verify Vercel has all public Supabase variables and server-only secrets.
- Run the Supabase migrations and confirm owner-only policies.
- Replace mock dashboard cards with user-scoped Supabase reads one page at a time.
- Add upload handling for Garmin CSV/FIT/TCX files.
- Add private Supabase Storage buckets for Garmin imports and progress photos.
- Add server-side provider routes for OpenClaw and AI Mentor calls.
- Add smoke tests for auth callback and API placeholders.
